using System;
using Godot;

namespace GlassesBar.Tests;

// Generated capture skeleton for ${BATCH_ID}. Replace the stop marker with a
// deterministic non-headless Forward+ sequence using only approved views and poses.
public partial class ${PASCAL_BATCH}AssetVisualCapture : Node
{
    public override void _Ready()
    {
        CallDeferred(MethodName.Prepare);
    }

    private void Prepare()
    {
        GetNode<Node3D>("Main");
        throw new InvalidOperationException(
            "RAISE_UNTIL_FORWARD_PLUS_CAPTURE_DEFINED: stage reality, glasses-world, wrapper, hand, scale, and context frames from the approved batch review plan"
        );
    }
}
