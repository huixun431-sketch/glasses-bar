# 核心交互物品建模计划表

- 更新日期：2026-08-12
- 状态：执行中；阶段 1／2 已完成，正式酒吧环境已替换 `bar_counter`，第一阶段正式配方资产扩展已排入后续批次
- 制作假设：单人负责建模、材质、锚点、导出与 Godot 交互校准
- 原 16 项基线预计工期：28–39 个工作日；第一阶段正式配方扩展须在尺寸卡完成后另行估时
- 建模基线：Blender 4.5.5 LTS

## 1. 目标与边界

本计划首先覆盖 `assets/asset_manifest.json` 中原有 16 项交互资产，并在第 8 节追加由第一阶段 9 杯正式配方反推的 42 个稳定资产 ID（折叠三种量酒器后为 40 个模型族）。视觉基线来自《美术设计案》：低多边形复古 3D、远距离可识别、轮廓明确、手绘基础色、AO 辅助和简单 PBR 参数；现实世界以暖色生活感为主，眼镜世界通过同一几何的材质覆盖表现低饱和、冷静和时间痕迹。

本计划不包含人物、酒柜、吊灯、墙面、地板等非清单环境资产。9 杯配方与明确用量已经批准；容差、概率／恢复曲线、成本、奖励与最终美术仍未批准。设计案中的 `500–10000` 面是单件资产的指导范围，不是硬性目标；增加面数不能替代轮廓识别或交互准确性。

计划于 2026-08-02 开始执行；阶段 1 四项与阶段 2 五项已完成建模、手写包装、运行与截图验收，`bar_counter` 已由正式环境批次接管。其余 6 项原计划资产仍是灰盒。第一阶段正式配方扩展只获得了内容与排期授权，尚未开始新的建模批次，不得把计划项误报为模型完成。

## 2. 状态与负责人规则

统一负责人记为“单人建模/技术美术”。每项资产只能使用以下状态：

- `未开始`：尚未建立尺寸卡或草模。
- `草模中`：正在确认尺度、轮廓、原点和接触面。
- `制作中`：进行正式低模、UV、材质和锚点制作。
- `技术验收`：检查单位、轴向、变换、命名、锚点和 GLB。
- `美术评审`：检查风格、材质语言、磨损和双世界表现。
- `交互验收`：在 Godot 检查握持、摆放、倾倒、液面、碰撞和命中范围。
- `M1 候选`：全部验收通过，但灰盒仍保留到批次运行复核结束。
- `待返修`：任一道验收门失败并记录明确返修原因。

## 3. 总体排期

| 阶段 | 工期 | 资产范围 | 主要产物 | 阶段通过条件 |
|---|---:|---|---|---|
| 0. 制作基线 | 2–3 日 | 全部 16 项 | Blender 模板、尺寸卡、轴向/命名/导出规则、材质基线 | 16 项均有灰盒尺度、接触面、原点、锚点和碰撞预期；不得直接凭目测开正式模型 |
| 1. 风格技术样件 | 6–8 日 | `highball_glass`、`jigger_medium`、`mortar`、`pestle` | 玻璃、金属、复合材质样件与首轮 GLB | 完成两轮 Godot 校准和两轮美术评审；未通过不得批量生产后续资产 |
| 2. 手持核心工具 | 5–7 日 | `traditional_filter`、`bean_scoop`、`ice_tongs`、`jigger_small`、`jigger_large` | 其余手持工具与量酒器同族变体 | 手持遮挡、倾倒方向、内容转移、摆放稳定和远距识别均通过 |
| 3. 固定交互站点 | 7–9 日 | `water_kettle`、`ice_bucket`、`bar_sink`、`coffee_beans`、`waste_bin` | 固定站点视觉 GLB | 出水点、液面、抽屉净空、取料焦点、投放范围及站点碰撞通过 |
| 4. 承载结构 | 4–5 日 | `cutting_board`（`bar_counter` 已由正式环境批次完成） | 砧板候选 | 三槽砧板、台面接触面、柜体通行和既有布局边界保持兼容 |
| 5. 集成与返修 | 4–7 日 | 全部 16 项 | 包装场景接入、双世界复核、返修记录 | 完整流程、错误操作、左右手、三种量酒器、资产验证器和双世界截图全部通过 |

原 16 项基线按 28–39 个单人工作日管理。第一阶段正式配方扩展的 40 个模型族不得沿用该旧估时，须在 3A 尺寸卡与共模策略确认后另行估算。每个生产阶段包含约 20% 的返修缓冲；缓冲不能挪用来跳过验收门。

## 4. 逐项任务表

### 阶段 1｜风格技术样件

| 顺序 | 资产 ID | 负责人 | 当前状态 | 前置依赖 | 锚点 | 交互精度重点 | 美术评审重点 |
|---:|---|---|---|---|---|---|---|
| 1 | `highball_glass` | 单人建模/技术美术 | `M1 候选` | 阶段 0 尺寸卡 | `Grip`、`Placement`、`FillOrigin` | 杯底接触面、握持姿态、透明液面基准、第一人称遮挡 | 低多边形透明玻璃、边缘识别、现实暖光与眼镜冷色覆盖 |
| 2 | `jigger_medium` | 单人建模/技术美术 | `M1 候选` | 材质基线、量酒器尺寸卡 | `Grip`、`Placement`、`FillOrigin`、`Spout` | 双头方向、换端姿态、取水/倾倒轴、两端液面基准 | 暗银/金属候选、刻度或轮廓识别方式；最终材质在本批锁定 |
| 3 | `mortar` | 单人建模/技术美术 | `M1 候选` | 砧板槽尺寸卡 | `Grip`、`Placement`、`FillOrigin`、`Interaction` | 左手握持、砧板落位、容积空间、与研杵组合净空 | 复古复合材质、口沿和底座轮廓、适度使用痕迹 |
| 4 | `pestle` | 单人建模/技术美术 | `M1 候选` | `mortar` 草模 | `Grip`、`Placement`、`Interaction` | 右手握持、研磨轴向、与研钵内壁/砧板槽净空 | 与研钵成套但保持独立轮廓、接触端磨损合理 |

阶段 1 在第一次技术草模后进行样件美术评审，在首次 Godot 交互校准后进行返修复审。四件样件全部达到 `M1 候选` 前，不进入阶段 2 批量生产。

阶段 1 已于 2026-08-02 通过造型与材质两轮用户审核、多轮 Godot Forward+ 校准及完整自动化回归；详细记录见 `docs/assets/STAGE1_ASSET_BATCH_20260802.md`。阶段 2 也已于同日通过轮廓与 Godot Forward+ 两个用户检查点并完成收尾，详见 `docs/assets/STAGE2_ASSET_BATCH_20260802.md`；阶段 3 尚未开始。

### 阶段 2｜手持核心工具

| 顺序 | 资产 ID | 负责人 | 初始状态 | 前置依赖 | 锚点 | 交互精度重点 | 美术评审重点 |
|---:|---|---|---|---|---|---|---|
| 5 | `traditional_filter` | 单人建模/技术美术 | `M1 候选` | 阶段 1 风格锁定 | `Grip`、`Placement`、`Spout`、`Interaction` | 左手握持、过滤出口、砧板落位、与高球杯组合位置 | 传统器具轮廓须单独确认，不自行固化未批准造型 |
| 6 | `bean_scoop` | 单人建模/技术美术 | `M1 候选` | 金属材质基线 | `Grip`、`Placement`、`FillOrigin` | 右手取豆、一次只载一种原料、装载可视区域 | 与量酒器区分轮廓，原料勺身份可远距辨认 |
| 7 | `ice_tongs` | 单人建模/技术美术 | `M1 候选` | 金属材质基线、冰桶净空卡 | `Grip`、`Placement`、`Interaction` | 右手握持、夹取方向、抽屉冰桶净空、载冰落台限制 | 夹口和弹性轮廓清晰，不依赖细小纹理识别 |
| 8 | `jigger_small` | 单人建模/技术美术 | `M1 候选` | `jigger_medium` 批准母版 | `Grip`、`Placement`、`FillOrigin`、`Spout` | 小号双头容量方向、换端、取水和倾倒 | 与中号同族且尺寸/色值可辨，不发明正式容量外观规则 |
| 9 | `jigger_large` | 单人建模/技术美术 | `M1 候选` | `jigger_medium` 批准母版 | `Grip`、`Placement`、`FillOrigin`、`Spout` | 大号双头容量方向、换端、取水和倾倒 | 与中号同族且尺寸/色值可辨，保持低多边形语言 |

阶段 2 于 2026-08-02 通过用户轮廓检查点和 Godot Forward+ 实机检查点。五项清单已切换为非占位并通过完整回归；它们是已批准的 M1 技术／材质候选，不是最终美术定稿。阶段 3 的 5 个固定站点与阶段 4 的 `cutting_board` 仍为灰盒；`bar_counter` 已由正式环境批次接管。

### 阶段 3｜固定交互站点

| 顺序 | 资产 ID | 运行时实体 ID | 负责人 | 初始状态 | 锚点 | 当前灰盒包络参考 | 交互精度重点 |
|---:|---|---|---|---|---|---|---|
| 10 | `water_kettle` | `kettle` | 单人建模/技术美术 | 未开始 | `Placement`、`FillOrigin`、`Spout`、`Interaction` | `0.44 × 0.48 × 0.40 m` | 固定水源交互焦点、壶嘴出水点、液面基准；不得改成手持水壶玩法 |
| 11 | `ice_bucket` | `ice_bucket` | 单人建模/技术美术 | 未开始 | `Interaction` | `0.62 × 0.25 × 0.48 m`，位于指定上层抽屉 | 抽屉开合净空、冰夹命中区域、关闭时不可交互 |
| 12 | `bar_sink` | `hand_wash_sink` | 单人建模/技术美术 | 未开始 | `Interaction`、`Spout` | `1.45 × 0.16 × 0.62 m` | 每日洗手焦点、龙头出水点、水槽下方净空；不得恢复制作用水功能 |
| 13 | `coffee_beans` | `coffee_beans` | 单人建模/技术美术 | 未开始 | `Placement`、`Interaction` | `0.58 × 0.42 × 0.42 m` | 原料勺取豆焦点、后吧台可见性、载料反馈位置 |
| 14 | `waste_bin` | `waste_bin` | 单人建模/技术美术 | 未开始 | `Placement`、`Interaction` | `0.72 × 1.10 × 0.72 m` | 手动清废投放范围、侧面通行、近距离命中区域 |

资产 ID 与运行时实体 ID 必须保持上述映射。GLB 文件名仍按资产清单使用 `water_kettle.glb` 与 `bar_sink.glb`，不得为了匹配站点 ID 而重命名玩法实体或资产稳定 ID。

### 阶段 4｜承载结构

| 顺序 | 资产 ID | 负责人 | 初始状态 | 前置依赖 | 锚点 | 当前灰盒参考 | 交互精度重点 |
|---:|---|---|---|---|---|---|---|
| 15 | `cutting_board` | 单人建模/技术美术 | 未开始 | 阶段 1、2 工具落位数据 | `Placement`、`Interaction` | 工作板 `2.05 × 0.14 × 0.72 m`，三槽间距沿用布局定义 | 三槽组合、工具接触面、内容转移焦点、连续防重叠摆放 |
| 16 | `bar_counter` | 单人建模/技术美术 | 正式环境接管 | 全部站点/工具包络 | `Placement` | 前台表面约 `10.30 × 0.08 × 0.84 m`；台面高 `1.42 m` | 已由正式酒吧环境提供台面、柜体与碰撞；后续只做兼容性复核，不重复建模 |

## 5. 六道验收门

每件资产必须按顺序通过，失败时进入 `待返修` 并记录问题、负责人、目标修复批次和复验结果。

### 5.1 尺度与轮廓草模

- 对照当前灰盒尺寸、第一人称视角和相邻物体建立尺寸卡。
- 确认底部接触面、原点、主要朝向和远距离剪影。
- 传统滤具等尚无最终造型的物品必须先获得造型确认，再进入正式制作。

### 5.2 技术验收

- 米制，应用缩放与旋转，Godot 前向 `-Z`、上方 `+Y`。
- 文件名、根节点和稳定 ID 与 `asset_manifest.json` 一致。
- 仅包含所需网格、材质和锚点；不得在导入节点上绑定玩法脚本。
- 锚点名称大小写必须精确匹配 `Grip`、`Placement`、`Spout`、`FillOrigin`、`Interaction`。

### 5.3 美术评审

- 检查低多边形复古语言、远距识别和明确轮廓。
- 检查木材、铜/黄铜/暗银、玻璃等材质归属；采用手绘基础色、AO 辅助和简单 PBR 参数。
- 磨损必须服务于“经营多年、被时间留下痕迹”，不得使用无目的随机噪点堆叠。
- 现实层检查暖色生活感；眼镜层默认复用几何并通过材质覆盖表现低饱和、冷静和细节暴露。
- 设计案中的“透明量杯”仅作为通用示例；当前双头量酒器先制作金属候选，最终材质在阶段 1 评审锁定。

### 5.4 交互验收

- 按资产适用性检查左右手握持、手持遮挡、摆放稳定、砧板槽位和普通台面防重叠。
- 容器检查 `FillOrigin`、可视容积和内容物不穿模；倾倒工具检查 `Spout` 与操作轴向。
- 固定站点检查交互焦点、抽屉/柜门净空、投放/取料范围和玩家通行。
- 交互校准不得改变配方、动作许可、站点 handler、提示、数值或玩法状态归属。

### 5.5 接入验收

- GLB 只作为手写玩法包装场景下的视觉子场景。
- 包装场景持有碰撞体、稳定 ID、必要的锚点修正、交互脚本和眼镜材质覆盖。
- `tools/validate_assets.py assets/asset_manifest.json --allow-placeholders` 必须保持 0 错误。
- 对应候选只有在运行与视觉复核通过后才能去掉 `placeholder`；灰盒在此之前继续作为回退。

### 5.6 运行验收

- 完整覆盖接单前自由制作、正确冰美式流程、错误工具/材料、比例偏差、手动清废和重做。
- 覆盖左右手、三种量酒器换端、研钵/研杵组合、冰夹/冰桶、量酒器/水壶、滤具/高球杯组合。
- 现实世界与眼镜世界分别截取实际 Forward+ 画面，检查尺度、可读性、材质、灯光、模糊和交互提示。
- 自动化通过不能替代主观握持手感、美术风格和第一人称遮挡验收。

## 6. 批次记录模板

每批执行时复制以下记录，不允许仅口头宣布通过：

| 字段 | 记录内容 |
|---|---|
| 批次/日期 | 例如：阶段 1 / YYYY-MM-DD |
| 资产 ID | 本批涉及的稳定 ID |
| 当前状态 | 使用第 2 节状态枚举 |
| 产物 | GLB、纹理、外部归档的 `.blend` 源文件、包装场景 |
| 尺度与技术结果 | 通过/失败，附问题摘要 |
| 美术评审结果 | 通过/返修，附批准方向 |
| 交互验收结果 | 握持、摆放、倾倒、液面、碰撞、焦点逐项记录 |
| 自动化证据 | 资产验证、构建、领域/输入/流程测试结果 |
| 视觉证据 | 现实/眼镜截图路径及人工检查结论 |
| 灰盒回退 | 保留/允许替换 |
| 下一动作 | 明确负责人和目标批次 |

## 7. Git 与资产交付规则

- 建模源文件由美术侧归档；未另行批准前，不把 `.blend` 源文件直接纳入游戏仓库。
- 游戏仓库只接收批准的 GLB、纹理、资产清单路径、包装场景和验收记录。
- 每件资产必须保持稳定 ID；替换占位模型只能通过资产清单和包装场景完成，不改写配方或交互逻辑。
- 本计划文档的首次提交仅包含计划与进展交接文档，不包含现有 `export_presets.cfg` 用户改动，也不推送。

## 8. 第一阶段正式配方资产扩展（2026-08-12）

### 8.1 正式配方来源

本节以 `data/recipes/stage1_recipe_catalog.tres` 为权威数据源。九杯配方是冰美式、金汤力、古典鸡尾酒、莫吉托、玛格丽特、莫斯科骡子、代基里、马天尼和威士忌酸。配方正文未写入的威士忌酸橙皮装饰不纳入资产需求；水、咖啡豆、苏打水、柠檬汁和蛋白虽未出现在用户原库存汇总中，但因配方正文需要而补入。

以下统计口径必须分开：

- **42 个稳定资产 ID**：每个独立玩法包装或可追踪资源形态一个 ID。
- **40 个模型族**：三种量酒器共享一个模型族、使用三个稳定 ID，因此比稳定 ID 少 2。
- **约 40 个可交互物品**：是早期规模估算，不等同于精确 GLB 数量；水、液体内容物、果汁与切分状态可能以材质／子网格／粒子资源承载。
- **26 个成分形态、23 个采购来源族**：是配方数据口径，不等同于模型资产口径。

### 8.2 去重后的稳定资产清单

| 批次 | 稳定资产 ID | 模型族 | 当前状态 | 复用／制作说明 |
|---|---|---|---|---|
| A 酒瓶 | `bottle_whiskey`、`bottle_gin`、`bottle_white_rum`、`bottle_vodka`、`bottle_tequila`、`bottle_dry_vermouth`、`bottle_orange_liqueur` | 7 | 未开始 | 共用瓶体技术基线，但酒标、瓶肩和轮廓至少有可辨分组；均需 `Grip/Placement/Spout/FillOrigin` |
| B 辅助瓶罐 | `bottle_aromatic_bitters`、`bottle_simple_syrup`、`bottle_tonic_water`、`bottle_ginger_beer`、`bottle_soda_water` | 5 | 未开始 | 糖浆瓶只计一次；汤力水、姜汁汽水和苏打水独立内容／标签变体 |
| C 果物植物 | `lime_set`、`orange_peel`、`mint_bunch`、`olive_jar`、`lemon`、`egg_white_container` | 6 | 未开始 | `lime_set` 统一承载完整果、片、角和榨汁前后状态；不为三种青柠形态重复建整套模型 |
| D 糖盐冰 | `sugar_cube_container`、`salt_container`、`ice_bucket`、`ice_resource_set` | 4 | `ice_bucket` 灰盒，其余未开始 | `ice_resource_set` 统一承载大冰块与碎冰资源；盐边是杯具状态，不另算独立工具 |
| E 杯具 | `highball_glass`、`rocks_glass`、`margarita_glass`、`copper_mug`、`cocktail_glass`、`sour_glass`、`iced_coffee_glass` | 7 | `highball_glass` 已正式，其余未开始 | 高球杯供金汤力／莫吉托；冰美式独立杯型，避免把现有高球杯误报为全部杯具完成 |
| F 调酒工具 | `jigger_small`、`jigger_medium`、`jigger_large`、`bar_spoon`、`cocktail_shaker`、`ice_scoop`、`bar_knife`、`cutting_board`、`ice_tongs` | 9 ID／7 模型族 | 三种量酒器与冰夹已正式；砧板灰盒；其余未开始 | 三种量酒器同族变体，容量锁定 `10/20`、`15/30`、`25/50 ml`；冰夹只计一次；莫吉托捣压复用现有正式研钵／研杵，不新增用户清单外工具 |
| G 咖啡设备 | `coffee_beans`、`coffee_grinder`、`coffee_extractor`、`water_kettle` | 4 | `coffee_beans`、`water_kettle` 灰盒；设备未开始 | 磨豆与萃取各需正式设备；现有豆铲／研钵／研杵／传统滤具只作为当前部分实现桥接，不计入正式配方库存 |

合计：`7 + 5 + 6 + 4 + 7 + 9 + 4 = 42` 个稳定资产 ID；三种量酒器折叠为一个模型族后是 40 个模型族。

### 8.3 现有资产复用与退役边界

- 已有正式、直接复用（5 个稳定 ID）：`highball_glass`、`jigger_small`、`jigger_medium`、`jigger_large`、`ice_tongs`。
- 已有灰盒、纳入后续替换（4 个稳定 ID）：`ice_bucket`、`cutting_board`、`coffee_beans`、`water_kettle`。
- 新增正式建模需求：33 个稳定 ID。
- 原计划中的 `bar_sink` 与 `waste_bin` 仍需正式替换，但属于酒吧基础站点，不计入 42 个配方物品资产。
- `mortar` 与 `pestle` 可在莫吉托捣压中正式复用；`traditional_filter`、`bean_scoop` 以及研钵／研杵在冰美式中仍是部分实现桥接。待 `coffee_grinder`、`coffee_extractor` 接入并通过运行／视觉验收后，才可从冰美式流程退役相应桥接，期间不得删除其灰盒回退或稳定 ID。

### 8.4 建议生产批次与前置依赖

| 新批次 | 范围 | 前置依赖 | 通过条件 |
|---|---|---|---|
| 3A 基础站点续作 | `ice_bucket`、`coffee_beans`、`water_kettle`、`bar_sink`、`waste_bin`、`cutting_board` | 现有灰盒包络与柜体净空 | 原计划阶段 3/4 技术、交互、Forward+ 检查点全部通过 |
| 3B 杯具与冰 | 其余 6 种杯具、`ice_resource_set`、`ice_scoop` | 玻璃基线、液面与握持基线 | 七种杯具远距可辨；大冰块／碎冰不混淆；冰铲可搬运碎冰 |
| 3C 基酒与辅助瓶 | 7 个酒瓶 + 5 个辅助瓶罐 | 瓶体族尺寸卡、酒架净空、标签规范 | 倾倒锚点、液面、标签可读性、货架陈列与双世界材质通过 |
| 3D 果物与调味 | 6 个果物／植物 + 方糖容器 + 盐罐 + `bar_knife` | 切割状态机、砧板槽位、研钵／研杵捣压工序设计 | 青柠切分、盐边、薄荷捣压与装饰物抓取通过 |
| 3E 摇调工具与咖啡设备 | `bar_spoon`、`cocktail_shaker`、`coffee_grinder`、`coffee_extractor` | 搅拌／摇酒／磨豆／萃取玩法接口 | 四类操作动画、内容转移、失败恢复与第一人称遮挡通过 |
| 3F 九杯整体验收 | 42 个稳定资产 ID | 3A–3E 均通过各自检查点 | 九杯工序、错误操作、双世界、完整回退、Forward+ 截图与自动化回归全部通过 |

每个新批次都从 `design` 阶段开始，必须重新经过中性轮廓检查点和 Godot Forward+ 检查点；本节不构成任何资产的自动批准，也不允许提前修改 `asset_manifest.json` 的 `placeholder`。

### 8.5 动画与交互依赖矩阵

| 动画／操作 | 主要资产 | 玩法依赖 | 当前状态 |
|---|---|---|---|
| 拿取与倾倒 | 全部酒瓶、辅助瓶罐、三种量酒器 | `Grip`、`Spout`、`FillOrigin`、实量液体转移 | 量酒器实量转移已实现；瓶体未开始 |
| 切割青柠 | `lime_set`、`bar_knife`、`cutting_board` | 切分状态、刀具连续动作、砧板落位与错误恢复 | 计划中 |
| 捣碎薄荷 | `mint_bunch`、`mortar`、`pestle` | 内容转移、捣压强度、过度／不足反馈 | 研钵／研杵可复用；薄荷与正式工序计划中 |
| 摇酒 | `cocktail_shaker` | 双手或受控单手动作、封盖状态、内容物混合 | 计划中 |
| 搅拌 | `bar_spoon`、相应杯具 | 搅拌时长／强度、冰与液体反馈 | 计划中 |
| 磨豆 | `coffee_beans`、`coffee_grinder` | 两勺共 18g、磨豆动作和咖啡粉输出 | 当前研钵／研杵桥接；正式设备计划中 |
| 咖啡萃取 | `coffee_extractor`、`water_kettle`、`iced_coffee_glass` | 100ml 水、萃取输出、失败恢复与液面 | 当前传统滤具桥接；正式设备计划中 |
