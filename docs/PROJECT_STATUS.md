# 项目状态

更新日期：2026-07-20

## 已完成

- 垂直切片技术方案与范围锁定。
- Godot 4.7.1 .NET/C# 工程骨架、灰盒世界和核心系统代码已建立。
- 双世界表现、第一人称输入、数据驱动配方、参数化液体与教学流程已实现到代码层。
- 项目级 Codex Skill、资产规范、资产清单与验证工具已建立。
- Blender 4.5.5 LTS 已安装到 `D:\Applications\Blender 4.5 LTS\`；后台启动、Python API、glTF/GLB 导出接口和 RTX 5070 CUDA/OptiX 识别已验证。
- .NET Debug/Release 构建均通过，0 警告、0 错误；纯领域测试 4/4 通过。
- Godot 4.7.1 .NET 首次导入、主场景无头启动和基础冒烟测试通过。
- 输入链路测试通过：输入映射、射线接单、切镜、戴镜移动与交互门控已验证。
- 流程集成测试通过：100 次切镜、状态保持、溢出、丢弃重做、制作、评价与日结已验证。
- 资产验证器好/坏 GLB 自测通过；7 项灰盒占位清单无结构错误。
- 项目 Skill 结构校验和隔离前向测试通过。
- 现实层与眼镜层已使用 Forward+ Movie Maker 渲染检查：HUD 清晰、客人/原料隐藏规则与异世界标签正确。
- 已安装 Godot 4.7.1-stable .NET Windows x86_64 Debug/Release 官方导出模板，并通过归档 CRC、版本标识和解压尺寸校验。
- 已补充 `GlassesBar.sln` 以满足 Godot 4.7.1 .NET 导出要求，并从导出资源中排除 `tests/*`。
- Windows Debug/Release 包已分别生成到 `builds/windows/debug/` 与 `builds/windows/release/`；两个包均已无头启动，未出现 `ERROR` 或未处理异常。
- 已修正戴镜移动：眼镜世界允许人物移动，但顾客、原材料和制作交互保持禁用；实际位移与交互门控测试通过。
- 已根据实际游玩反馈把玩家出生点移入吧台内侧工作区并朝向前台；输入集成测试确认初始射线可命中客人。
- 已扩充灰盒酒馆布局：正面工作台、后吧台酒架/酒瓶轮廓、卡座、夜景窗和分层暖光已接入现实/眼镜双表现，仍保留为后续美术替换的开发占位。
- 已实现宽容交互探测、可用/不可用原因提示、准星状态、操作进度条与短时结果确认条；靠近并朝向交互处即可获得明确 UI 反馈。
- 已实现参数化现实世界近视后处理：初始 50 度，戴镜世界清晰；按 `` ` `` 打开内置开发控制台，使用 `myopia <0-1000>` 可运行时调节。
- 已使用 Forward+ Movie Maker 实际检查现实、眼镜、交互反馈与控制台画面；修复了首轮截图发现的提示面板错误拉伸。
- 输入集成测试新增吧台内出生、宽容探测、提示/反馈、50 度默认值、Shader 运行时更新和控制台命令覆盖，测试通过。
- Windows Debug/Release 包已重导出并实际无头启动；导出预设新增排除 `artifacts/*` 与 `builds/*`，避免把本地视觉验证和旧构建产物打进分发包。
- Git 仓库已初始化为 `main`，本轮用户授权的基线提交包含当前已验证成果；构建目录与本机导入缓存保持忽略。

## 待人工/内容验收

- 新版出生点、交互宽容度、提示节奏和 50 度默认模糊仍需用户再次实际游玩确认；自动化与截图验证不能替代主观手感验收。
- 当前酒馆仍是参考预览图方向的灰盒布局，不是最终美术、最终灯光或固定陈设方案。
- 正式冰美式配方与容差尚未由策划提供。
- 首批美术和建模资产尚未交付。

## 计划中

- 由用户在新版 Windows 包中完成一次真实键鼠流程和操作手感复核，按反馈继续调整出生点、提示距离、UI 节奏和模糊强度。
- 使用固定的 Blender 4.5.5 LTS 开始首批物品建模，并按资产规范交付 GLB。
- 接入正式配方资源和首批 GLB。
