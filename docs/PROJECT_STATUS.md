# 项目状态

更新日期：2026-08-12

## 当前里程碑

正式酒吧环境批次与公开仓库 P0 收尾均已完成。M1 正式内容接入仍在进行中，后续工作不再包含酒吧环境检查点，而集中在六项独立灰盒资产、正式玩法内容和真实游玩验收。

## 已完成

### 正式酒吧环境

- 六个模块已正式接入：`bar_architecture`、`bar_counter`、`bar_backbar`、`bar_furniture`、`bar_lighting`、`bar_wear_overlays`。
- 六项在 `assets/asset_manifest.json` 与 `data/assets/asset_manifest.tres` 中均为非占位。
- 默认游戏使用正式环境；任一模块缺失时事务式清理并整体回退到完整可玩灰盒。
- 导入 GLB 只负责视觉；碰撞、稳定 ID、柜门／抽屉状态、14 盏逻辑灯和双世界共享玩法状态由 Godot 包装层拥有。
- 用户已批准现实暖色与眼镜冷色的最终 Forward+ 灯光候选。24 张 `1920×1080` 生产集成图已人工检查。

### 工具与玩法技术基线

- 阶段 1／2 共九项工具资产已通过各自视觉检查点并正式接入：高球杯、三种量酒器、研钵、研杵、传统滤器、豆铲、冰夹。
- 第一人称移动、双手实体工具、柜体与储物、数据驱动工序、参数化液体、每日洗手、量酒器取水、有限重复补救、评价、日结和 30 天原型循环已实现。
- 权威玩法状态与表现层已经分离；现实／眼镜世界共享唯一状态，眼镜世界提供观察和规划信息而不自动执行操作。
- Schema version 1 状态快照与内存往返已实现；它不是正式磁盘存档产品。

### 公开仓库

- `main` 已同步至公开 GitHub 仓库。
- 根目录 README 已包含项目定位、环境、运行、输入、验证、目录和贡献边界。
- 项目使用标准 MIT License，版权行为 `2026 Glasses Bar contributors`。
- `.gitattributes` 将 GLB 设为二进制、将 `.glb.import` 固定为 LF，已消除无内容差异的导入元数据噪声。
- 用户本地 `export_presets.cfg` 修改继续保留并排除提交。

## 最近验证

- `tools/run_verification.ps1`：退出码 `0`。
- 资产：`21` 项，`0` 错误；六项正式环境 GLB 全部 `OK`。
- C#：Debug/Release `0` 警告、`0` 错误；领域测试 `28/28`。
- Godot：布局、储物、运行时几何、正式表现、生产资产／强制回退、冒烟、Stage 1/2、输入和完整流程场景全部 PASS。
- 建模批次：严格 `phase=complete errors=0`。

## 未完成

### 需要正式资产

- `ice_bucket`
- `bar_sink`
- `cutting_board`
- `water_kettle`
- `coffee_beans`
- `waste_bin`

以上六项继续明确保持灰盒，尚未启动新的建模批次。

### 需要用户／策划决策

- 正式冰美式配方、容量、容差和操作手册内容。
- 概率／恢复曲线、卫生影响、评价、奖励与成本。
- 正式酒类／饮品列表、顾客内容和最终美术方向。

现有相关值仍是开发占位，不得表述为最终平衡。

### 需要实际游玩

- 基于当前正式酒吧重新生成测试包并连续游玩至少两天。
- 检查菜单输入切换、玩家眼高、柜门／抽屉通行、冰桶／水壶／量酒器／洗手流程、现实／眼镜可读性、每日复位和错误恢复。
- 旧 `2026-07-23` 灰盒测试包不代表当前空间、模型和灯光。

### 后续功能

- 操作手册拿取、打开、阅读、翻页与放回表现。
- 正式磁盘存档槽、原子写入、备份、迁移、损坏回退、“继续游戏”和设置持久化。
- 顾客 AI、后台区域、多顾客、随机订单、经营经济、升级和补货。
- M2 第二杯饮品与 M3 经营扩展。

这些均不属于已经完成的 P0 范围，开始前需要新的明确任务。
